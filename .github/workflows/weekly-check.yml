name: Weekly submission check

on:
  pull_request:
    paths:
      - "student-submissions/**"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Validate submission format
        run: bash scripts/validate_submission.sh